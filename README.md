# Лабораторная работа 7. Анализ и преобразование кода с использованием Clang и LLVM

## Цель

Познакомиться с инструментарием Clang и LLVM, освоить получение абстрактного синтаксического дерева (AST) и промежуточного представления (LLVM IR) для кода на C/C++, научиться применять базовые оптимизации, строить графы потока управления (CFG), а также анализировать влияние оптимизаций на различные синтаксические конструкции языка.

## Постановка задачи:
1. Установка среды
2. Работа с AST
3. Генерация LLVM IR
4. Оптимизация IR
5. Построение CFG
6. Индивидуальное задание (по варианту)

## Установка среды (1)
```bash
sudo apt install clang llvm graphviz
```

<img width="636" height="482" alt="image" src="https://github.com/user-attachments/assets/09917cb0-9700-4056-8302-deed8eaa9541" />


Создание и переход в рабочую папку:

<img width="649" height="47" alt="image" src="https://github.com/user-attachments/assets/f1f54297-4186-4db2-9ecd-2629f1ff6184" />


## Общее задание (2-5)
Заданная программа на языке C
```bash
#include <stdio.h>

int square(int x) {
    return x * x;
}

int main() {
    int a = 5;
    int b = square(a);
    printf("%d\n", b);
    return 0;
}
```
# Получение AST
Команда 
```bash
clang -Xclang -ast-dump -fsyntax-only main_example.c
```
Результат:

<img width="1107" height="814" alt="image" src="https://github.com/user-attachments/assets/3171ca68-b871-44b5-90e4-d02f869ff337" />

# Генерация LLVM IR
Команда 
```bash
clang -S -emit-llvm main_example.c -o main_example.ll
```
Результат:

<img width="1191" height="706" alt="image" src="https://github.com/user-attachments/assets/532a98ff-3a77-4510-a0f0-901d23e799dc" />


# Оптимизация IR (-O0 и -O2)
Команда 
```bash
clang -O0 -S -emit-llvm main_example.c -o main_example_O0.ll
```
До оптимизации все переменные размещены в памяти через alloca, множество операций load и store, а также square вызывается как отдельная функция.

Результат:

<img width="1280" height="721" alt="image" src="https://github.com/user-attachments/assets/117a41d5-c90d-483d-b731-71c366ed1f09" />

---

Команда 
```bash
clang -O2 -S -emit-llvm main_example.c -o main_example_O2.ll
```
После оптимизации вся функция square исчезла – она была встроена (-inline) и затем вычислена (оптимизация -constprop), далее никаких переменных, alloca, store, load – всё удалено (оптимизации -mem2reg, -dce), и остался только вызов printf(25).

Результат:

<img width="1280" height="723" alt="image" src="https://github.com/user-attachments/assets/a7a4c749-774f-4c62-b0c4-38ab5b9eef1b" />


# Построение CFG
Граф потока управления до оптимизации:
```bash
opt -passes=dot-cfg -disable-output main_example_O0.ll
dot -Tpng .main.dot -o main_example_cfg_O0.png
```
Граф потока управления после оптимизации:
```bash
opt -passes=dot-cfg -disable-output main_example_O2.ll
dot -Tpng .main.dot -o main_example_cfg_O2.png
```

Команда для просмотра файлов с CGF:
```bash
xdg-open main_example_cfg_O0.png
xdg-open main_example_cfg_O2.png
```

CFG до оптимизации (-O0):

<img width="773" height="441" alt="image" src="https://github.com/user-attachments/assets/70cf033f-1f59-417c-ad7f-6c7f91e35c3b" />


CFG после оптимизации (-O2):

<img width="653" height="439" alt="image" src="https://github.com/user-attachments/assets/3ac5b9cf-b9c4-4d33-9517-42d3f81f20d3" />


# Выводы общей части:
1. Инструментарий Clang позволяет эффективно извлекать и анализировать структуру исходной программы на всех этапах: от построения абстрактного синтаксического дерева (AST) до генерации промежуточного представления (IR) и визуализации графа потока управления (CFG), что обеспечивает прозрачность процесса компиляции.
2. Инфраструктура LLVM предоставляет мощный набор библиотек для глубокого анализа и автоматизированной оптимизации программного кода, позволяя управлять поведением компилятора на каждом уровне трансформации.
3. Использование IR как универсального связующего звена значительно упрощает разработку и внедрение пользовательских компиляторных трансформаций, обеспечивая независимость создаваемых оптимизаций от особенностей конкретной архитектуры целевого процессора.

---

# Индивидуальная часть работы:

# Вариант:
Цикл for

# Пример:
```bash
int main() {
  int sum = 0;
  for (int i = 0; i < 10; i++) {
    sum += i;
  }
  return sum;
}
```

# AST для цикла do-while (do_while.c)

<img width="649" height="49" alt="image" src="https://github.com/user-attachments/assets/0c62650a-df8f-41e1-b047-d76cb1270bed" />

<img width="995" height="744" alt="image" src="https://github.com/user-attachments/assets/1b9024ba-b718-45de-80eb-33fe6cadf85a" />


# Задания:
1. Получите IR для -O0.
2. Получите IR для -O2. Раскрыт ли цикл?
3. Примените -unroll-count=4 и сравните.
4. Постройте CFG.
5. Сделайте вывод об оптимизации циклов в LLVM.

---

# Задание №1 - Получите IR для -O0.
Команда
```bash
clang -O0 -S -emit-llvm main.c -o main_O0.ll
```

Результат:

<img width="1280" height="722" alt="image" src="https://github.com/user-attachments/assets/8555445d-bccb-4cc0-bb32-41c84045b8e2" />

# Задание №2 - Получите IR для -O2. Раскрыт ли цикл?
Команда
```bash
clang -O2 -S -emit-llvm main.c -o main_O2.ll
```

Результат:

<img width="1280" height="724" alt="image" src="https://github.com/user-attachments/assets/425e7ffb-8998-475b-aa0d-1c17ceb006ff" />

---
Цикл не раскрыт. Это произошло из-за того, что компилятор выполнил анализ скалярной эволюции и свертку констант. Поскольку все параметры цикла (начало, конец, шаг) были известны на этапе компиляции, LLVM понял, что цикл - это просто способ получения числа 45.
В итоге компилятор заменил сложную структуру с ветвлениями и инкрементами на одну инструкцию ret i32 45. Удаление цикла всегда приоритетнее его раскрытия, так как это полностью убирает накладные расходы на выполнение цикла.


# Задание №3 - Примените -unroll-count=4 и сравните.

Команда 
```bash
clang -S -emit-llvm -02 -mllvm -unroll-count=4 main.c
```

Результат:

<img width="1280" height="458" alt="image" src="https://github.com/user-attachments/assets/5e1a26f7-5d6a-458e-8497-2431498035cc" />

---

Оптимизация циклов в LLVM базируется на принципе приоритета математической эффективности над директивными инструкциями программиста. Когда компилятор распознает в цикле алгоритмический шаблон, поддающийся вычислению , он применяет полное удаление цикла и замену его на константный результат. В таком случае любые попытки принудительного раскрытия цикла игнорируются, так как раскрытый итеративный код объективно медленнее и занимает больше места в памяти, чем одна инструкция возврата значения. Таким образом, LLVM рассматривает цикл как черный ящик, который нужно либо полностью исключить, либо оптимизировать через развертку. Раскрытие цикла в IR наблюдается только тогда, когда компилятор лишен возможности статически вычислить результат, что делает ручное управление оптимизацией через флаги вторичным процессом по сравнению с мощным аналитическим ядром LLVM.

# Задание №4 - Постройте CFG.
Граф потока управления до оптимизации (O0):
```bash
opt -passes=dot-cfg -disable-output main_O0.ll
dot -Tpng .main.dot -o main_cfg_O0.png
```
---
Граф потока управления после оптимизации (O2):
```bash
opt -passes=dot-cfg -disable-output main_O2.ll
dot -Tpng .main.dot -o main_cfg_O2.png
```


---
# Результаты:


# До оптимизации (O0) - main_cfg_O0.png

<img width="792" height="865" alt="image" src="https://github.com/user-attachments/assets/802acf7d-454c-45cb-bfdf-a73c9c594ea8" />


---

# После оптимизации (O2) - main_cfg_O2.png

<img width="438" height="558" alt="image" src="https://github.com/user-attachments/assets/513b9230-fff4-4697-9ce7-8de535adb2af" />

---

# -unroll-count=4

<img width="438" height="191" alt="image" src="https://github.com/user-attachments/assets/1c08c36c-e543-4299-95f3-1292b6b606e9" />


# Задание №5 - Сделайте вывод об оптимизации циклов в LLVM.
Результаты работы подтверждают, что в LLVM раскрытие цикла является вторичной оптимизацией, применяемой только в тех случаях, когда компилятор не может статически вычислить результат. Таким образом, современный оптимизатор выступает в роли математического решателя, который в большинстве случаев превосходит ручные методы оптимизации кода, делая их избыточными или контрпродуктивными.
Использование агрессивных методов оптимизации в LLVM позволяет достичь максимальной производительности за счет минимизации накладных расходов на управление потоком выполнения, однако требует от разработчика понимания того, что структура исходного кода на языке высокого уровня не всегда соответствует структуре сгенерированного машинного кода.


# Задание №6 - Ответы на контрольные вопросы.
1. Что такое Clang, и какова его роль в процессе компиляции программ?
   
Clang - это фронтенд компилятора. Он необходим для работы с исходным текстом программы (C/C++). Он проверяет синтаксис, строит абстрактное синтаксическое дерево (AST) и переводит код в промежуточное представление (LLVM IR).

2. Что представляет собой LLVM и как он используется в современных компиляторах?
   
LLVM - это модульная инфраструктура для разработки компиляторов, выполняющая роль оптимизатора и бэкенда. Современные компиляторы используют её как конструктор - берут готовые библиотеки LLVM для оптимизации универсального кода и его перевода в машинные инструкции конкретного процессора (x86, ARM).

3. Чем отличается абстрактное синтаксическое дерево (AST) от промежуточного представления LLVM IR?
  
- AST - это высокоуровневое дерево структуры кода, привязанное к синтаксису языка C/C++.
- LLVM IR - это низкоуровневый линейный ассемблер, полностью независимый от исходного языка программирования и оперирующий базовыми блоками, метками и регистрами.

4. Для чего необходимо промежуточное представление (IR) в процессе компиляции?
   
IR избавляет от необходимости писать отдельный компилятор под каждую пару «Язык - Процессор». Вместо этого все языки переводятся в один общий IR. Оптимизации пишутся один раз для IR, и они автоматически работают для любого входного языка и любого целевого процессора.

5. Что делает инструкция alloca в LLVM IR, и зачем она используется в функциях?
   
Инструкция alloca выделяет память на стеке текущего кадра функции для локальной переменной. Она используется, чтобы гарантировать переменной её физический адрес в памяти для корректного выполнения операций чтения (load) и записи (store).

6. Зачем нужна оптимизация кода в компиляторе, и какие основные цели она преследует?
   
Оптимизация нужна для улучшения характеристик программы без изменения логики её работы. Она позволяет увеличить скорость выполнения, уменьшить размер бинарного файла и снизить потребление оперативной памяти или энергии.

7. Что такое SSA-форма и почему она важна при оптимизации программ?
   
SSA (Static Single Assignment) - это форма кода, где каждому виртуальному регистру значение присваивается ровно один раз. Если переменная в C++ меняется, в IR создаются новые регистры (%x1, %x2). При ветвлениях используется специальная ϕ-функция (Phi-node) для выбора нужного значения. SSA важна, так как она убирает ложные зависимости между инструкциями, делая оптимизации простыми и быстрыми.

8. Что такое граф потока управления (CFG) и как он помогает анализировать поведение программы?

CFG - это ориентированный граф функции, где:
- Вершины - базовые блоки (куски кода без ветвления внутри, выполняющиеся строго линейно).
- Ребра - стрелки переходов (br, switch) между ними.
  
Он помогает компилятору видеть логику переходов, находить циклы, оптимизировать условия и удалять недостижимый код.

9. Как устроено представление арифметических операций в LLVM IR (например, умножение, сложение)?
    
Они представлены в строго типизированной трехадресной форме - операция принимает два операнда фиксированного типа и записывает результат в новый регистр. Также могут быть флаги (например, nsw - отсутствие знакового переполнения).
Пример: %res = add nsw i32 %0, %1 (сложение двух 32-битных целых чисел).

10. Почему функции в LLVM IR обычно представляют собой отдельные единицы анализа и оптимизации?
    
Функции обладают локальностью: у них изолированные переменные, четкие аргументы и границы. Анализировать всю программу сразу слишком долго и ресурсоемко, а анализ отдельных функций позволяет компилятору работать быстро и эффективно.

11. Что происходит с функцией в LLVM IR, если она вызывается один раз и очень короткая?
    
При включении оптимизаций (например, -O2) оптимизатор применяет инлайнинг (Inlining). Тело этой короткой функции встраивается прямо в место её вызова, а сама функция удаляется. Это убирает накладные расходы процессора на прыжки по коду (call/ret) и сохранение регистров.

12. Какие преимущества даёт использование IR и CFG для автоматических оптимизаций по сравнению с анализом исходного текста на C?
    
В IR нет синтаксического разнообразия: любые циклы (for, while, do-while) в IR превращаются в одинаковые базовые блоки и переходы br.
Действует строгое правило SSA, благодаря которому легко отслеживать, откуда взялось значение в регистре.
Граф CFG дает четкую математическую модель связей в коде, которую невозможно построить простым анализом текста на С из-за сложных областей видимости и макросов.


# Дополнительное задание: 
## Оптимизация 1: свёртка констант диапазона цикла

### Текстовое описание

При генерации IR для цикла for (i in m:n) границы диапазона и число итераций задаются литералами, но во входном IR всё равно строится цепочка вычислений через служебные переменные: __m, __n, __t_sub = __n − __m, __trip = __t_sub + 1, а также присваивание i = __m и условие if i <= __n. Если оба конца диапазона известны на этапе компиляции, эти выражения вычисляются заранее: в IR подставляются константы (__trip = 10, i = 1, if i <= 10), а промежуточные строки с __m, __n и __t_sub удаляются. Переменная цикла i не сворачивается - она меняется в теле цикла.

### Преимущества

- Сокращение числа тетрад в прологе цикла 
- Устранение лишних служебных переменных __m, __n, __t_sub
- Вычисление числа итераций и границ диапазона на этапе компиляции, а не при выполнении программы

### Блок-схема

<img width="1179" height="714" alt="image" src="https://github.com/user-attachments/assets/a0ad94ca-c255-42d2-bcbd-207211f48457" />

### Входной и выходной IR (оптимизация 1)

**Входной IR** (до оптимизации 1):

<img width="1072" height="301" alt="image" src="https://github.com/user-attachments/assets/f747ed0e-0bf9-4c3f-8301-afe199b1d6e1" />


**Выходной IR** (после оптимизации 1):

<img width="1061" height="256" alt="image" src="https://github.com/user-attachments/assets/557687c8-409a-4e3c-b37b-7d5614fe8b43" />

Изменения: строки 1–4 объединены в одну строку 1; __m, __n и __t_sub больше не нужны. Строка 5 заменена на i = 1. В условии цикла __n заменён на литерал 10. Тело цикла без изменений.

---

## Оптимизация 2: прямой инкремент переменной цикла

### Текстовое описание

При генерации IR для шага цикла строится пара тетрад: сначала __t_inc = i + 1, затем i = __t_inc. Если временная __t_inc используется ровно один раз - сразу после вычисления, - пара заменяется одной тетрадой: результат записывается сразу в переменную цикла (+ i 1 i, то есть i = i + 1). Семантика цикла не меняется.

### Преимущества

- Сокращение числа тетрад в теле цикла 
- Устранение лишней временной переменной __t_inc
- Более компактный IR шага цикла без лишнего копирования

### Блок-схема

<img width="1086" height="698" alt="image" src="https://github.com/user-attachments/assets/dccd124f-a6c8-45a5-a175-a35413494fe4" />

### Входной и выходной IR (оптимизация 2)

**Входной IR** (до оптимизации 2):

<img width="1060" height="304" alt="image" src="https://github.com/user-attachments/assets/b80dcdf6-c9e7-4cf5-925a-a43e88a88764" />

**Выходной IR** (после оптимизации 2):

<img width="1073" height="237" alt="image" src="https://github.com/user-attachments/assets/0f612970-aeca-480b-ae79-a5132c85be09" />

Изменения: строки 5–6 объединены в одну строку 5; __t_inc больше не нужна. Условие цикла без изменений.

---


<img width="1916" height="1027" alt="image" src="https://github.com/user-attachments/assets/239d1ada-7c5f-4081-866a-5aff0c852fab" />
